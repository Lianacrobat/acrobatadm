---

---

<!-- Imágenes distribuidas uniformemente en la parte superior -->
<div
	class="pointer-events-none absolute top-0 left-0 right-0 z-10 h-40 sm:h-40 overflow-hidden w-full"
>
	<!-- Layout responsive: grid 2x2 en móvil, flex horizontal en desktop -->
	<div class="grid grid-cols-2 gap-2 p-2 w-full h-full sm:flex sm:justify-between sm:items-center sm:px-4 sm:py-2">
		<!-- Spring Floor -->
		<img
			src={`${import.meta.env.BASE_URL}assets/springFloor.png`}
			alt="Spring Floor"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>

		<!-- XPE -->
		<img
			src={`${import.meta.env.BASE_URL}assets/xpe.png`}
			alt="XPE"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>

		<!-- Arnes -->
		<img
			src={`${import.meta.env.BASE_URL}assets/arnes.png`}
			alt="Arnes"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>

		<!-- Podium -->
		<img
			src={`${import.meta.env.BASE_URL}assets/podium.png`}
			alt="Podium"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>
	</div>
</div>

<!-- Imágenes distribuidas uniformemente en la parte inferior -->
<div
	class="pointer-events-none absolute bottom-0 left-0 right-0 z-10 h-40 sm:h-40 overflow-hidden w-full"
>
	<!-- Layout responsive: grid 2x2 en móvil, flex horizontal en desktop -->
	<div class="grid grid-cols-2 gap-2 p-2 w-full h-full sm:flex sm:justify-between sm:items-center sm:px-4 sm:py-2">
		<!-- Spring Track -->
		<img
			src={`${import.meta.env.BASE_URL}assets/springTrack.png`}
			alt="Spring Track"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>

		<!-- Franjas -->
		<img
			src={`${import.meta.env.BASE_URL}assets/franjas.png`}
			alt="Franjas"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>

		<!-- Trampolin -->
		<img
			src={`${import.meta.env.BASE_URL}assets/trampolin.png`}
			alt="Trampolin"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>

		<!-- XPE -->
		<img
			src={`${import.meta.env.BASE_URL}assets/xpe.png`}
			alt="XPE"
			class="w-32 h-32 object-contain opacity-100 justify-self-center"
		/>
	</div>
</div>
